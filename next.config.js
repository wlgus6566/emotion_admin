const nextConfig = {
  reactStrictMode: true,
  /*async rewrites() {
    return [
      {
        source:  "/:path*",
        destination: `${process.env.BASE_URL}/:path*`
      },
    ];
  },*/
}

module.exports = nextConfig
